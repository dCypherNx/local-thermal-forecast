[project]
name = "local-thermal-forecast"
version = "0.3.2"
description = "Hybrid local weather and room temperature forecasts for Home Assistant"
requires-python = ">=3.14"

[tool.ruff]
line-length = 100
target-version = "py314"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
